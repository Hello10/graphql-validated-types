{
  "name": "graphql-validated-types",
  "version": "1.0.4",
  "description": "A collection of GraphQL custom scalars supporting cleanup, validation, and defaults via fluent api",
  "main": "index.js",
  "scripts": {
    "test": "$(npm bin)/mocha --ui bdd './src/**/*.test.js'",
    "example": "npm install && cd example && npm install && npm start"
  },
  "author": "Stephen Handley",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^3.5.0",
    "moment": "^2.18.1",
    "mongodb": "^2.2.31"
  },
  "dependencies": {
    "email-regex": "^1.0.0",
    "graphql": "^0.11.2",
    "ip-regex": "^2.1.0",
    "phone-regex": "^2.0.0",
    "url-regex": "^4.1.1"
  }
}
